# Convert a Bool to a String (C++)
Another overly simple challenge from [codeforce](https://codeforce.com), most of their challenges are a bit too easy but i do them for practice purposes, in this challenge we are asked to convert a bool to it's string representation (can't get easier than that).

